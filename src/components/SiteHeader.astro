---
import WipMark from "~/components/WipMark.astro";
import { BRAND_NAME } from "~/lib/constants";

interface Props {
  homeHref?: string;
}

const { homeHref = "/" } = Astro.props;
---

<div class="fixed end-6 top-6 z-50 hidden sm:block">
  <a href={homeHref}>
    <h1 class="sr-only">{BRAND_NAME}</h1>
    <WipMark />
  </a>
</div>

<div class="fixed end-6 top-6 z-50 sm:hidden">
  <a href={homeHref} class="inline-block p-2.5 -m-2.5">
    <img
      src="/logo.svg"
      height="24"
      alt="WIP home"
      class="h-6 w-auto invert dark:invert-0"
    />
  </a>
</div>
